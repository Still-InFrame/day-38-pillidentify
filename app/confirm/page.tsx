import { AppFrame } from "@/components/pillcheck/AppFrame";
import { ConfirmationForm } from "@/components/pillcheck/ConfirmationForm";

export default function ConfirmPage() {
  return (
    <AppFrame>
      <ConfirmationForm />
    </AppFrame>
  );
}
